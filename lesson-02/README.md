# Lesson 02: Filtering Rows

**Promise:** by the end of this lesson you can keep exactly the rows you want
— by value, range, pattern, and membership — and you'll know why comparing
against `NULL` never works (and how to test for it correctly).

**You need from before:** Lesson 01 (`SELECT`, `FROM`, `ORDER BY`, `LIMIT`).

**Keywords this lesson:** `WHERE`, `=`, `<>`, `<`, `>`, `<=`, `>=`,
`AND`, `OR`, `NOT`, `IN`, `NOT IN`, `BETWEEN`, `NOT BETWEEN`, `LIKE`,
`IS NULL`, `IS NOT NULL`

**Files in this folder:** [lesson.md](lesson.md) (read and do this),
[answers.md](answers.md) (quiz key — open only after you've attempted the
quiz).
